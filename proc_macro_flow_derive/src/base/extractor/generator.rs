// @review [ ]
// TODO(#generator/pipeline):C[S(GeneratorPipeline)], "Bare struct holding its own extractor/processor/generator triple for the generator stage"
// TODO(#generator/expansion):C[F(expand)], "expand(&GeneratorPipeline) -> TokenStream first, concretely; only then wire the outer expansion that emits the final generated code (the impl/TokenStream the StructExtraction pipeline ultimately produces). Two separate passes - don't conflate the inner macro-of-a-macro with the outer codegen"
// TODO(#generator/macro):C[F(generator)], "Proc-macro entry point for the generator stage, alongside lib.rs::extractor"

// Query(#generator/base-scope):Q[this ??], "What should the base Generator::generate_visitor actually emit - just the ItemImpl shape, or the full derive expansion body? Decide before wiring #generator/expansion"
// Answer(#generator/scope-reply):A[ID(generator/base-scope) ==? ID(syntax/render)], "Partially answered by the syntax stage: whatever it emits, it must emit it EVEN WHEN the extraction failed. A generator that returns only compile_error!s leaves the impl missing, and the resulting 'does not implement' cascade at every use site buries the real diagnostic. So the floor is a stub ItemImpl beside the errors. Whether the base also emits the full body, or only the shape for a concrete generator to fill, stays open until ID(generator/expansion)"
// DEPRECATED(#generator/visitor):D[S(Generator)] && D[F(generate_visitor)], "Deleted. The struct held an ExtractorProcessor, which went with ID(processor/bare-source), and generate_visitor took `target: ()` with an unimplemented!() body - a placeholder whose only job was letting the crate compile. Neither encoded a decision worth carrying: what a generator emits is still open at ID(generator/base-scope), and the one thing already settled about it - that it must emit a stub impl ALONGSIDE the errors, so a missing impl does not cascade into 'does not implement' at every use site - is recorded in the Answer below and needs no placeholder to hold its place"
